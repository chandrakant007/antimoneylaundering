<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/util
           https://www.springframework.org/schema/util/spring-util.xsd
           http://www.springframework.org/schema/jee
           https://www.springframework.org/schema/jee/spring-jee.xsd
           http://www.springframework.org/schema/aop
           https://www.springframework.org/schema/aop/spring-aop.xsd
           http://www.springframework.org/schema/tx
           https://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- ========================================================= -->
    <!-- DataSource Bean -->
    <!-- ========================================================= -->
    <bean id="sessionSessionFactory"
          class="com.aml.amlbatchprocess.ck.config.ckDBLogin"
          destroy-method="close">

        <property name="url" value="jdbc:yourdb://localhost:port/dbname"/>
        <property name="username" value="db_username"/>
        <property name="dbPassword" value="db_password"/>
    </bean>

    <!-- ========================================================= -->
    <!-- Hibernate SessionFactory -->
    <!-- ========================================================= -->
    <bean id="amlSessionFactory"
          class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">

        <property name="dataSource" ref="sessionSessionFactory"/>

        <!-- Annotated Entity Classes -->
        <property name="annotatedClasses">
            <list>
                <value>com.example.entity.YourEntity</value>
                <!-- add more entity classes here -->
            </list>
        </property>

        <!-- Hibernate Properties -->
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">org.hibernate.dialect.MySQLDialect</prop>
                <prop key="hibernate.show_sql">true</prop>
                <prop key="hibernate.format_sql">true</prop>
                <prop key="hibernate.jdbc.batch_size">50</prop>
            </props>
        </property>
    </bean>

    <!-- ========================================================= -->
    <!-- Transaction Manager -->
    <!-- ========================================================= -->
    <bean id="txManager"
          class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="amlSessionFactory"/>
    </bean>

    <!-- ========================================================= -->
    <!-- Transaction Template -->
    <!-- ========================================================= -->
    <bean id="transactionTemplate"
          class="org.springframework.transaction.support.TransactionTemplate">
        <property name="transactionManager" ref="txManager"/>
    </bean>

    <!-- Enable annotation-driven transactions (optional but recommended) -->
    <tx:annotation-driven transaction-manager="txManager"/>

</beans>
